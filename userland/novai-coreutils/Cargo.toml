[package]
name        = "novai-coreutils"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
authors     = { workspace = true }
description = "NovaiOS coreutils facade — dispatches to uutils-coreutils when present"
publish     = false

[[bin]]
name = "novai-coreutils"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap   = { workspace = true }
which  = { workspace = true }
